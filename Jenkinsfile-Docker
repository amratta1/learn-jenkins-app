pipeline {
    agent any 
    stages {  
        stage('Build Docker image'){
          steps{
             sh '''
                docker build -f ./cid/Dockerfile -t my-playwright .
               '''
         }
    }
 }
}
